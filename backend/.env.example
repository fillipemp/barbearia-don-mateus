# Copie este arquivo para .env e ajuste os valores

PORT=3001

# true  = usa banco local (data.json) — para desenvolvimento/testes locais
# false = usa Firebase Firestore     — para produção
USE_LOCAL=true
